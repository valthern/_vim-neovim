📝 EJERCICIO 1: SUSTITUIR TEXTO EN UNA LÍNEA
1.Utiliza el comando `:s` para sustituir todas las ocurrencias de la palabra "user" por "usuario"
SELECT usuario_id, usuario_nombre, usuario_correo, usuario_pais, usuario_edad FROM usuario;

2. Utiliza el comando `:s` para cambiar todas las ocurrencias de la palabra "boton" por "btn"
<button class="btn btn-primario btn-grande">Este es un botón</button>


📝 EJERCICIO 2: SUSTITUIR EN TODO EL DOCUMENTO
Utiliza `:%s` para cambiar la palabra "nombre" por "nombre" y "correo" por "correo" en todo el documento.

const usuario1 = {
	nombre: 'Carlos',
	correo: 'correo@correo.com'
}

const usuario2 = {
	nombre: 'Alejandro',
	correo: 'alejandro@correo.com'
}

const usuario3 = {
	nombre: 'Manuel',
	correo: 'manuel@correo.com'
}



📝 EJERCICIO 3: SUSTITUIR CON CONFIRMACIÓN
1. Utiliza el modo visual para seleccionar la función "obtenerDatos()".
2. Utiliza el comando `:s` con la bandera `/gc` para sustituir la palabra "datos" por la palabra "respuesta".
3. Salta la sustitución del nombre de la función "obtener_datos" y la invocación "obtener_datos()".

function async obtener_datos(){
	const respuesta = await fetch('https://www...');
	if(respuesta.status === 'OK' && respuesta.code === 200){
		return(respuesta.body);
	}
}
obtener_datos();
